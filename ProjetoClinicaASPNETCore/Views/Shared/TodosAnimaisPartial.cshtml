@model Animal

@functions {
    public string ModalConsultasAnimal()
    {
        var modal = $"ModalConsultasAnimalId{@Model.AnimalId.ToString()}"; ;
        return modal;
    }
    public string ModalDonoAnimal()
    {
        var modal = $"ModalDonoAnimalId{@Model.AnimalId.ToString()}"; ;
        return modal;
    }
    public string ModalExcluir()
    {
        var modal = $"modalExcluirId{@Model.AnimalId.ToString()}"; ;
        return modal;
    }

    public string GetDateLocal(string toConvert)
    {
        if (toConvert != null)
        {
            var date = toConvert;
            DateTime convertedDate = Convert.ToDateTime(date);
            return convertedDate.ToShortDateString();
        }
        else
            return "";
    }
}

<tr class="text-center">
    <td>@Model.AnimalId</td>
    <td>@Model.User.NomeCompleto</td>
    <td>@Model.AnimalNome</td>
    <td>@Model.AnimalTipo</td>
    <td>@Model.AnimalRaca</td>
    <td>@GetDateLocal(Model.AnimalDataDeNascimento)</td>
    <td>@Model.Alergico</td>
    <td>
        <a  class="btn btn-outline-primary" asp-controller="AdminAnimal" asp-action="Editar"
                   asp-route-animalId="@Model.AnimalId">
            <i class="fas fa-edit"></i>
        </a>
    </td>
    <td>
        <button type="button" class="btn btn-outline-primary" data-toggle="modal" data-target="#@ModalConsultasAnimal()">
            <i class="fas fa-eye"></i>
        </button>
    </td>
    <td>
        <button type="button" class="btn btn-outline-primary" data-toggle="modal" data-target="#@ModalDonoAnimal()">
            <i class="fas fa-eye"></i>
        </button>
    </td>
    <td>
        <button type="button" class="btn btn-outline-danger" data-toggle="modal" data-target="#@ModalExcluir()">
            <i class="fa fa-eraser"></i>
        </button>
    </td>
    @*
        <td>
            <a asp-controller="Editar" asp-action="Edit" asp-route-id="@Model.FormularioId">Editar</a>
        </td>*@
</tr>

<div class="modal fade" id="@ModalConsultasAnimal()" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Consultas (@Model.AnimalNome @Model.AnimalId)</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body text-center">
                @foreach (Consulta consulta in Model.Consultas)
                    {
                    <div>
                        <div class="lead">Consulta (@consulta.ConsultaId)</div>
                        Data da Consulta: @GetDateLocal(consulta.DataConsulta)<br />
                        Horario da Consulta: @consulta.HorarioConsulta<br />
                        Descrição do Problema: @consulta.DescricaoDoProblema<br />
                        Valor da Consulta: @consulta.ValorConsulta<br />
                        Diagnóstico da Consulta: @consulta.Diagnostico<br />
                        @if(consulta.IsVerificado == true) { <div>Consulta Verificada: Sim</div> } else { <div>Consulta Verificada: Não</div>}
                        @if(consulta.IsConcluido == true) { <div>Consulta Concluída: Sim</div> } else { <div>Consulta Concluída: Não</div>}
                    </div>
                }
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-dismiss="modal">Voltar</button>
                @*<a class="btn btn-outline-success" asp-controller="AdminAnimais" asp-action="Delete"
                   asp-route-idAnimal="@Model.AnimalId">
                    Editar
                </a>*@
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="@ModalDonoAnimal()" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Dono do Animal (@Model.AnimalNome @Model.AnimalId)</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body text-center">
                <div class="lead">Dados do dono:</div>
                Identificador: @Model.User.Id<br />
                Nome: @Model.User.NomeCompleto<br />
                CPF: @Model.User.CPF<br />
                Telefone: @Model.User.PhoneNumber<br />
                Email: @Model.User.Email<br />
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-dismiss="modal">Voltar</button>
                @*<a class="btn btn-outline-success" asp-controller="AdminAnimais" asp-action="Delete"
                   asp-route-idAnimal="@Model.AnimalId">
                    Editar
                </a>*@
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="@ModalExcluir()" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Excluir animal</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body text-center">
                <div class="lead">INFORMAÇÕES DO ANIMAL</div>
                Animal: @Model.AnimalId<br />
                Nome do Animal: @Model.AnimalNome<br />
                Dono do Animal: @Model.User.NomeCompleto<br /> <br />
                <div class="lead">ATENÇÃO</div>
                Todas as consultas deste animal serão excluídas!<br />
                Número de Consultas: @Model.Consultas.Count().ToString()
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-dismiss="modal">Voltar</button>
                <a class="btn btn-outline-success" asp-controller="AdminAnimais" asp-action="Delete"
                   asp-route-idAnimal="@Model.AnimalId">
                    Excluir
                </a>
            </div>
        </div>
    </div>
</div>
